{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make Program",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${fileDirname}"
            }
        },
        {
            "label": "Make Clean",
            "type": "shell",
            "command": "make",
            "args": ["clean"],
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ]
}